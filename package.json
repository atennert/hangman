{
  "name": "hangman",
  "version": "0.0.5",
  "description": "A simple JS hangman implementation",
  "main": "./src/index.html",
  "scripts": {
    "test": "NODE_ENV=test ./node_modules/mocha-webpack/bin/mocha-webpack --webpack-config ./webpack.test.config.js --require test/setup.js test/*.spec.js",
    "lint": "npm-run-all lint:**",
    "lint:ts": "tslint ./src/**/*.ts",
    "lint:css": "sass-lint -v -q --max-warnings 0",
    "prebuild": "rm -rf ./public/$npm_package_version && mkdir -p ./public/$npm_package_version",
    "postbuild": "rm -rf ./tmp",
    "build": "npm-run-all --parallel build:**",
    "build:ts": "webpack && uglifyjs ./tmp/app.js > ./public/$npm_package_version/index.js && uglifyjs ./tmp/sw.js > ./public/$npm_package_version/sw.js",
    "build:css": "sassc src/style.scss | postcss -c .postcssrc.json | cssmin > ./public/$npm_package_version/style.css",
    "build:html": "cp ./src/*.html ./public/$npm_package_version/",
    "build:manifest": "cp ./src/manifest.json ./public/$npm_package_version/",
    "build:icon": "convert -resize 144x144 -background none ./src/icon.svg ./public/$npm_package_version/icon.png"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/atennert/hangman.git"
  },
  "keywords": [
    "hangman",
    "game"
  ],
  "author": "Andreas Tennert",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/atennert/hangman/issues"
  },
  "homepage": "https://hangman.andreas-tennert.de",
  "devDependencies": {
    "@types/node": "^8.0.53",
    "autoprefixer": "^7.1.6",
    "chai": "^4.1.2",
    "cssmin": "^0.4.3",
    "mocha": "^4.0.1",
    "mocha-webpack": "^1.0.1",
    "npm-run-all": "^4.1.2",
    "postcss-cli": "^4.1.1",
    "sass-lint": "^1.12.1",
    "ts-loader": "^3.2.0",
    "ts-node": "^3.3.0",
    "tslint": "^5.8.0",
    "typescript": "^2.6.2",
    "uglify-es": "^3.3.3",
    "webpack": "^3.10.0"
  },
  "dependencies": {}
}
